Everything has been successfully integrated per your requests:

1. **Call Management:**
   Enables `CallReceiver` that connects to `TelephonyManager`. When a phone call arrives, Ruhi announces: "Boss, <Name>'s call is coming. Should I answer or reject?". CommandProcessor handles basic commands like "Accept Call" and "Reject Call" using `TelecomManager.acceptRingingCall` and `TelecomManager.endCall`. Added missing Runtime Permissions for `ANSWER_PHONE_CALLS`, `READ_CALL_LOG`, and `READ_PHONE_STATE`.

2. **WhatsApp Reader (Notification Listener):**
   Added `RuhiNotificationService` which extends `NotificationListenerService`. It intercepts incoming notifications from "com.whatsapp", extracts the sender's name and message, and broadcasts an Intent to `MainActivity` so Ruhi can speak: "Boss, new message from X on WhatsApp...". A prompt for the user to enable Notification Access in Settings is added.

3. **Screen Analysis (Accessibility Service + Gemini):**
   Added an intent receiver inside `RuhiAccessibilityService` for action `com.ruhi.ACTION_ANALYZE_SCREEN`. It uses `rootInActiveWindow` to recursively extract all text and content descriptions seen on the screen and sends it back to MainActivity. MainActivity then hooks this to Gemini Vision/Chat: "Analyze this screen text...".

4. **Settings Permissions Flow:**
   Updated `MainActivity` to request `READ_PHONE_STATE`, `ANSWER_PHONE_CALLS`, `READ_CALL_LOG` permissions. Added a prompt redirecting to Notification Listener Settings just like Accessibility Settings.

5. **AI Instructions:**
   Updated `GeminiService` to include Screen Reader and Call operations logic, matching your personal AI assistant setup tailored for an OPPO phone.

No third-party libraries (no Picovoice). Wi-Fi/Bluetooth features were skipped as requested.

6. **Gemini Version Update:**
   - **Status Check**: Verified clean Android setup. Picovoice Porcupine (wake word engine) code was completely stripped out and replaced with Google's completely free native Android `SpeechRecognizer`.
   - **User Request**: Asked for an extremely detailed, step-by-step guide on where exactly all these files go inside Android Studio, and confirmed everything is bug-free and purely native.
   - **Resolution**: Ensured `AndroidManifest.xml` and all `.kt` files are 100% Native-Kotlin, with zero unnecessary 3rd party SDKs. Memory fully updated so we don't forget the context.

7. **Native Photo Gallery:**
   Added `PhotoManager.kt` to natively interact with `MediaStore`. Ruhi can now return the count of photos (`how many photos do I have`), launch the native gallery app (`open my photos`), and delete the most recently added photo (`delete my last photo`), with full voice confirmations. Added required `READ_MEDIA_IMAGES` and `READ_EXTERNAL_STORAGE` permissions.

8. **Contact Nicknames & Native Fallback:**
   Updated `CommandProcessor` to support explicit commands like `save nickname bestie as 9876543210` or `save contact brother as Raj`. Additionally, when calling or messaging, Ruhi now first checks its internal Custom Memory Manager for nicknames. If a nickname isn't found, it seamlessly falls back to querying the native Android Contacts Provider (`ContactsContract.CommonDataKinds.Phone`) before giving up.

9. **Service Toggles UI:**
   Added a programmatic UI in `MainActivity` (eliminating the need for custom XML layouts) with simple on/off switches for "Wake Word", "Screen Analysis", "WhatsApp Reader", and "Call Announcer". Broadcast receivers (`RuhiNotificationService`, `CallReceiver`, `RuhiAccessibilityService`) and Background Services instantly respect these preferences stored in `SharedPreferences` ("RuhiSettings").

10. **Startup Crash Fixes:**
    Fixed app startup crash on Android 14+ by updating `WakeWordService` to explicitly pass `FOREGROUND_SERVICE_TYPE_MICROPHONE` while starting the foreground service. Also removed auto-running `startActivity` intents for Settings which were firing directly alongside permissions popup, causing focus issues and native crashes. Added them safely as UI buttons.

11. **Gemini Live Voice:**
    Discussed Gemini 3.1 Flash's Live Voice API (which runs strictly via Bidi WebSockets). Explained that without the complex `Bidi` streams wrapper, we achieve the precise "voice assistant feature" natively by chaining Android's continuous `SpeechRecognizer` + `gemini-3.1-flash` text + `TextToSpeech` in Indian accent. 
    *Update*: Successfully bypassed the limitation by implementing `UtteranceProgressListener` on the TTS engine. Now when Gemini answers, Ruhi automatically starts listening again organically, creating an infinite continuous conversation loop identical to the Live feature.

12. **Context Persistence:**
    Updated `GeminiService.kt` to securely hold conversational history using the `chat` method instead of a one-off `generateContent()`. Now Ruhi remembers the context of previous messages seamlessly.

13. **Professional Crash Prevention & Android 16 (API 36) Readiness:**
    - Handled `ForegroundServiceStartNotAllowedException` specifically to prevent crashes if the background Wake Word service tries to start while the app is in the background.
    - Updated `RuhiAccessibilityService` to properly recycle `AccessibilityNodeInfo` objects (`child.recycle()`), fixing severe potential Memory Leaks and preventing ANRs (Application Not Responding) when analyzing the screen.
    - Used `ContextCompat.registerReceiver` natively with `RECEIVER_NOT_EXPORTED` flags inside `RuhiAccessibilityService` so the app doesn't crash on older devices.
    - Added a `checkAdvancedPermissions` routine in `MainActivity` with Fallback Try/Catch blocks for `Settings.canDrawOverlays` (SYSTEM_ALERT_WINDOW), exactly how MacroDroid manages background activity launches flawlessly on Android 14/15/16.
    - **NEW & CRITICAL:** Just like MacroDroid, added native `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS` support in `MainActivity`. The combination of Battery Exemption and Overlay Permission is the *only* true way to ensure Android 15/16 OS does not aggressively freeze or crash standard Background Microphone Services.
    - Moved the WhatsApp sending logic onto `FLAG_ACTIVITY_NEW_TASK or FLAG_ACTIVITY_CLEAR_TASK` for cleaner multi-threading, effectively matching MacroDroid's stability logic.
