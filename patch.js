const fs = require('fs'); let content = fs.readFileSync('src/services/commandService.ts', 'utf8'); content = content.replace('url?: string;', 'url?: string;\n  isOfflineFallback?: boolean;'); const offlineFallback = \'  if (typeof navigator !== \\'undefined\\' && !navigator.onLine) {\n    if (lowerCmd.includes(\\'hello\\') || lowerCmd.includes(\\'hi ruhi\\')) {\n        return { action: \"Hello Boss. I am currently offline, but listening.\", isBrowserAction: true, isOfflineFallback: true };\n    }\n    if (lowerCmd.includes(\\'how are you\\') || lowerCmd.includes(\\'kya haal hai\\') || lowerCmd.includes(\\'kese ho\\')) {\n        return { action: \"I am fine Boss, but my internet connection seems down.\", isBrowserAction: true, isOfflineFallback: true };\n    }\n    if (lowerCmd.includes(\\'time\\') || lowerCmd.includes(\\'samay\\')) {\n        const timeStr = new Date().toLocaleTimeString([], { hour: \\'2-digit\\', minute: \\'2-digit\\' });\n        return { action: \`The time is ${timeStr}, Boss.\`, isBrowserAction: true, isOfflineFallback: true };\n    }\n    return { action: \"Boss, my internet is off right now, so I can only do basic things.\", isBrowserAction: true, isOfflineFallback: true };\n  }\n\n  return { action: \"\", isBrowserAction: false };\n\'; content = content.replace('return { action: "", isBrowserAction: false };', offlineFallback); fs.writeFileSync('src/services/commandService.ts', content);